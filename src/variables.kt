// variable is a conatiner that stores data

fun main(){
    // var is mutable variable and its value can change
    // val is immutable and its value cannot change

   /* var name="kotlin"
    print("My name is $name")

    name="Kotlin new"
    print("Now, Mynew name is $name")
    */

    val country="Canada"
    print("I live in $country")

    //Tip: Use VAL by default and only use var when you know the value will change
}

